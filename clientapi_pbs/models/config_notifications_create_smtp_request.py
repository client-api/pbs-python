# coding: utf-8

"""
    Proxmox Backup Server API

    Generated from apidoc.js. NOT an official Proxmox specification. See https://pbs.proxmox.com/docs/api-viewer/ for the upstream documentation.

    The version of the OpenAPI document: 9.x
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from clientapi_pbs.models.pbs_config_notifications_mode_enum import PbsConfigNotificationsModeEnum
from clientapi_pbs.models.pbs_origin_enum import PbsOriginEnum
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ConfigNotificationsCreateSmtpRequest(BaseModel):
    """
    ConfigNotificationsCreateSmtpRequest
    """ # noqa: E501

    author: Optional[StrictStr] = Field(default=None, description="Author of the mail. Defaults to 'Proxmox Backup Server ($hostname)'")

    comment: Optional[Annotated[str, Field(strict=True, max_length=128)]] = Field(default=None, description="Comment.")

    disable: Optional[StrictBool] = Field(default=None, description="Disable this target.")

    from_address: StrictStr = Field(description="`From` address for the mail. SMTP relays might require that this address is owned by the user in order to avoid spoofing. The `From` header in the email will be set to `$author <$from-address>`.", alias="from-address")

    mailto: Optional[List[Annotated[str, Field(min_length=2, strict=True, max_length=64)]]] = Field(default=None, description="Mail address to send a mail to.")

    mailto_user: Optional[List[Annotated[str, Field(min_length=2, strict=True, max_length=64)]]] = Field(default=None, description="Users to send a mail to. The email address of the user will be looked up in users.cfg.", alias="mailto-user")

    mode: Optional[PbsConfigNotificationsModeEnum] = Field(default=None, description="Connection security")

    name: Annotated[str, Field(min_length=2, strict=True, max_length=32)] = Field(description="Name schema for targets and matchers")

    origin: Optional[PbsOriginEnum] = Field(default=None, description="The origin of a notification configuration entry.")

    password: Optional[StrictStr] = Field(default=None, description="SMTP authentication password")

    port: Optional[Annotated[int, Field(le=65535, strict=True, ge=0)]] = Field(default=None, description="The port to connect to. If not set, the used port defaults to 25 (insecure), 465 (tls) or 587 (starttls), depending on the value of mode")

    server: StrictStr = Field(description="Host name or IP of the SMTP relay.")

    username: Optional[StrictStr] = Field(default=None, description="Username to use during authentication. If no username is set, no authentication will be performed. The PLAIN and LOGIN authentication methods are supported")

    __properties: ClassVar[List[str]] = ["author", "comment", "disable", "from-address", "mailto", "mailto-user", "mode", "name", "origin", "password", "port", "server", "username"]



    @field_validator('comment')
    def comment_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[[:^cntrl:]]*$", value):
            raise ValueError(r"must validate the regular expression /^[[:^cntrl:]]*$/")
        return value







    @field_validator('name')
    def name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^(?:[A-Za-z0-9_][A-Za-z0-9._\-]*)$", value):
            raise ValueError(r"must validate the regular expression /^(?:[A-Za-z0-9_][A-Za-z0-9._\-]*)$/")
        return value








    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ConfigNotificationsCreateSmtpRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            # `exclude_unset` keeps schema defaults out of the wire payload
            # when the user constructed the model directly (e.g.
            # `Req(vmid=100)` would otherwise pull in
            # `cores=1, cpulimit=0, …` from the spec defaults and PVE
            # rejects the request with 400 because it never set those).
            # `exclude_none` keeps None values out of the wire payload —
            # both for direct construction (None means "unset") and for
            # the from_dict path (where unspecified obj keys become
            # `obj.get("k") == None` but show up in `model_fields_set`).
            exclude_unset=True,
            exclude_none=True,
        )
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ConfigNotificationsCreateSmtpRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "author": obj.get("author"),
            "comment": obj.get("comment"),
            "disable": obj.get("disable"),
            "from-address": obj.get("from-address"),
            "mailto": obj.get("mailto"),
            "mailto-user": obj.get("mailto-user"),
            "mode": obj.get("mode"),
            "name": obj.get("name"),
            "origin": obj.get("origin"),
            "password": obj.get("password"),
            "port": obj.get("port"),
            "server": obj.get("server"),
            "username": obj.get("username")
        })
        return _obj


